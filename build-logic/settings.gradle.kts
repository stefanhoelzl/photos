// The build's own build: the task types and plugins behind `:native` and its consumers.
dependencyResolutionManagement {
    repositories { gradlePluginPortal() }
}

rootProject.name = "build-logic"
