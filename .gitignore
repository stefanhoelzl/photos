INGEST.md
mockups/index.html
# The desktop app's cache: shards, packs and browsed blobs for this checkout (§4).
.cache/
.build/
.tools/

# Gradle
build/
.gradle/
.kotlin/
local.properties

# Test scratch that escaped into the tree once (see TestScratch.kt)
photos-roundtrip-*
photos-tests-*
